# vecini.online - DEMO stage committed env.
# Frontend-only, no backend. Safe to commit: no secrets.
# Run with: npm run dev:demo (or npm run build:demo)
VITE_APP_STAGE=demo
VITE_DEFAULT_LOCALE=ro
VITE_SECURITY_ENFORCEMENT=relaxed
